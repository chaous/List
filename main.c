#include <stdio.h>
#include "List.h"
#include "UI.h"


int main()
{
    //List list;
    //initList(&list, 'c');
    //char a = 'a', b = 'b';
    //addElem(&list, &a);
    //addElem(&list, &b);
    //int a = 1, b = 2, c = 3;
    //addElem(&list, &a);
    //addElem(&list, &b);
    //addElem(&list, &a);
    //delElmByNumber(&list, 1);
    getInput();

    //printList(&list);
    //printf("%d\n", lengh(&list));
    //printf("%d\n", isSorted(&list));
    //destroyList(&list);
    return 0;
}
